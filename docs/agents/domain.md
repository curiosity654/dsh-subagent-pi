# Domain Docs

This repository uses a single-context domain layout.

Before exploring the codebase, read `CONTEXT.md` when present and inspect
relevant ADRs under `docs/adr/`. If either is absent, proceed silently.

Use the domain vocabulary defined in `CONTEXT.md`. Surface any conflict with an
existing ADR rather than silently overriding it.
